function  convertirEdad (edad)  { 

    if ( edad  >=  18 )  { 
        console.log ( `Eres mayor de edad. ${edad}`) 
    } else {
        console.log( `Eres menor de edad. ${edad}`) 
    }
   
}
convertirEdad (20)